cabs:
  wsclean:
    backend:
      select: native
    command: ~/src/wsclean/bin/wsclean

# use virtual_env breifast for now
lib:
  misc:
    breifast:
      # common options for breifast cabs can set en masse here
      cabs:
        # use local install of breifast for now
        backend:
          select: native

    suricat:
      # common options for suricat cabs can set en masse here
      cabs:
        # use local install of suricat for now
        backend:
          select: native

