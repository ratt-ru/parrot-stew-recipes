# use virtual_env breifast for now
lib:
  misc:
    breifast:
      # common options for breifast cab can set en masse here
      cabs:
        # use virtual_env breifast for now
        backend:
          select: native

    suricat:
      # common options for breifast cab can set en masse here
      cabs:
        # use virtual_env suricat for now
        backend:
          select: native

